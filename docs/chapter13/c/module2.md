# 第 13 章：大模型的基本训练流程 — 模块 2：监督微调（SFT）

> 📍 学习进度：第 13 章，第 2 / 5 模块
> 📅 生成时间：2026-06-03

---

## 学习目标

- 理解 SFT 在训练流程中的定位：预训练（续写模型）→ SFT（问答模型）的转变
- 掌握两种主流 SFT 数据格式：Alpaca（单轮）与 ChatML/ShareGPT（多轮），及其适用场景
- 认识"数据质量 > 数据数量"的核心论断及其量化证据（1k 高质量 > 50k 普通标注）

---

## 核心内容

### 1. SFT 是什么？为什么需要它？

#### 1.1 定位：从"续写模型"到"问答模型"

预训练完成后，模型是一个**续写模型**——它只会根据上文预测下一个 token。你问它"法国的首都是哪里？"，它可能续写成"法国的首都是哪里？德国、意大利、西班牙..."——不是在回答，而是在**续写一段列举欧洲国家的文本**。

SFT（Supervised Fine-Tuning，监督微调）的作用就是**教会模型"问答格式"**：

```
预训练底座模型的问题：
  ✗ 只会续写，不会问答
  ✗ 可能输出有害或偏见内容
  ✗ 答案散漫、跑题
  ✗ 有严重的幻觉
  ✗ 不会角色扮演、工具调用

SFT 后期望的模型：
  ✓ 学会指令格式（QA 形式）
  ✓ 用户说一，模型不答二
  ✓ 学会 SFT 数据中的回答格式
  ✓ 学会拒绝有害内容
  ✓ 学会工具调用
```

**核心原理**：预训练模型已经掌握了通用知识，SFT 只需用少量高质量数据（10k~100k 条）来**塑造行为格式**。这就是预训练的意义所在——不需要从头标注海量数据，只需少量示范即可。

> 🌐 **补充（Web Search / OpenReview 2025）**：最新研究 **GRAPE**（The Best Instruction-Tuning Data are Those That Fit）提出了一个反直觉的发现：SFT 最有效的数据不一定是"质量最高"的数据，而是**与基座模型预训练分布最匹配**的数据。GRAPE 通过选择基座模型 perplexity 最低的 response 作为 SFT 目标，用 1/4.5 的数据量超越了强基线 6.1%。这意味着**数据选择标准正在从"绝对质量"转向"模型适配度"**。

#### 1.2 SFT 与预训练的关键差异

| | 预训练 | SFT |
|---|---|---|
| **数据量** | 海量（T 级别 tokens） | 少量（10k~100k 条） |
| **数据类型** | 无标注原始文本 | 问答对 / 指令-回复对 |
| **优化目标** | Next-token prediction | 同（但只在 output 部分计算 loss） |
| **核心作用** | 注入知识 | 塑造行为格式 |
| **数据成本** | 低（互联网抓取） | 高（需人工标注或专家撰写） |

---

### 2. SFT 数据格式：Alpaca 与 ChatML/ShareGPT

SFT 数据的核心是"给模型看**人写的标准回答**，让它模仿"。主流格式分为两类：单轮 Alpaca 和多轮 ChatML/ShareGPT。

> 🌐 **补充（Web Search / Red Hat 2025）**：Red Hat Developer 在 2025 年 8 月的指南中总结了 SFT 数据集的三种通用格式——Chat 格式（多轮对话列表）、Instruct 格式（prompt-response 对）、Text-only 格式（已预处理的格式化字符串）。这与课程的分类一致。关键实践建议：**如果模型已有 chat_template（通过 tokenizer_config.json），在 SFT 中必须复用同一模板**，确保训练和推理时模型看到的格式完全一致。

#### 2.1 Alpaca 格式（单轮 / 指令式）

每行一条 JSON，由 Stanford 在 2023 年提出，是单轮指令微调的事实标准：

```json
{
  "instruction": "翻译成英文",
  "input": "你好",
  "output": "Hello"
}
```

- `instruction`：任务描述
- `input`：用户问题（可为空——当 instruction 本身已完整如"写一首关于秋天的俳句"时，input 留空）
- `output`：**人工写的理想回答**

文件整体为 `.jsonl`（每行一条 JSON），**训练时只计算 `output` 部分的交叉熵损失**——instruction 和 input 部分不贡献 loss，模型只需学会"看到这种格式后，生成什么回答"。

**适用场景**：单轮 Q&A、分类任务、命令执行、代码生成——一个输入对应一个输出，无需对话上下文。

#### 2.2 ChatML / ShareGPT 格式（多轮对话）

把多轮对话按角色堆成数组，本质上是"对话历史"的直接表示：

**ChatML（OpenAI 风格）**：
```json
{
  "messages": [
    {"role": "system", "content": "你是客服助手"},
    {"role": "user", "content": "怎么修改收货地址？"},
    {"role": "assistant", "content": "请在订单详情页点击…"}
  ]
}
```

**ShareGPT 风格**（`from`/`value` 键）：
```json
{
  "conversations": [
    {"from": "human", "value": "怎么修改收货地址？"},
    {"from": "gpt", "value": "请在订单详情页点击…"}
  ]
}
```

两种风格的关键区别：

| | ChatML | ShareGPT |
|---|---|---|
| 角色键 | `role` / `content` | `from` / `value` |
| 系统提示 | 支持 `system` 角色 | 通常不内置 |
| 框架兼容 | OpenAI API、TRL | Axolotl、Unsloth 原生 |
| 训练框架 | TRL、LLaMA-Factory、Axolotl | Axolotl、Unsloth、LLaMA-Factory |

**训练时**：只对 `assistant`（或 `gpt`）角色的 token 计算损失，`user`/`system` 部分仅作为上下文。

**核心原则**：**训练格式必须与推理格式一致**。如果你用 Alpaca 格式训练但用 ChatML 推理，模型从未见过推理时的格式——这会导致性能下降甚至完全不可用。

#### 2.3 格式选择决策

```
                    多轮对话？
                   /        \
                 否          是
                 /            \
            Alpaca         需要 system prompt？
           单轮 Q&A          /           \
           命令执行         是             否
           分类任务         /               \
                      ChatML          ShareGPT
                   OpenAI 兼容      对话/澄清场景
```

---

### 3. 数据质量 > 数据数量：量化证据

预训练的经验是"数据越多越好"，这个思维惯性很容易延续到 SFT。但大量研究一致表明：**SFT 阶段，数据质量的优先级远高于数据数量**。

#### 3.1 核心证据链

**s1 论文（李飞飞团队，2025）**：
- 用 **1000 条**蒸馏自 Gemini-2.0-Flash-Thinking 的高质量推理样本（s1K），经难度/多样性/质量三重过滤
- 对 Qwen2.5-32B-Instruct 做 26 分钟 SFT（16×H100）
- 配合"预算强制"（budget forcing）解码策略
- 结果：在 MATH 和 AIME24 上**超越 o1-preview 最高达 27%**
- 训练云成本 ≈ **50 美元**
- > 🌐 **交叉验证（arxiv 2501.19393 + ACL Anthology 2025）**：1000样本、26分钟/16×H100、up to 27% 均双源确认。课程原文"打平甚至略超"实为低估——原论文声称"exceeds o1-preview by up to 27%"。

**From Quantity to Quality（NAACL 2024）**：
- 引入 **IFD（Instruction-Following Difficulty）** 指标——衡量模型"自己生成的回答"与"训练数据标准回答"的差距
- **5%~10% 的精选样本**即可在 Alpaca 和 WizardLM 上达到全量数据训练效果
- > 🌐 **交叉验证（arxiv 2308.12032 + GitHub tianyi-lab/Cherry_LLM）**：10% 数据比例双源确认。课程原文"9k 精选 > 50k 全量"中 9k 对应 WizardLM（~90k × 10%），Alpaca 场景为 ~5k。

**MergeIT（arXiv 2503.00034）**：
- 提出从"selection"到"merging"的范式转变——将语义相似的指令**合并**为更紧凑的数据
- 两阶段：① 主题感知过滤（去冗余、保留多样性）；② LLM-based 合并（合成更丰富紧凑的数据）
- > 🌐 **交叉验证（arxiv 2503.00034）**：论文核心是 merging 非 selection。课程原文"6k/1/11数据/追平65k"在搜索结果中未直接确认，保留为课程原文引用。

**1000 条 > 50000 条？** 这不是数学错误——这是在 SFT 阶段反复被验证的经验规律。

#### 3.2 为什么会这样？

传统经验"越多越好"在 SFT 中失效的根本原因：**模型参数完全由数据驱动——"学什么"决定"会什么"**。

劣质数据（错误标签、噪声、缺失、偏见）**不会被模型忘记**，反而会被参数记忆，导致：
- 性能下降（拟合错误模式）
- 泛化变差（噪声淹没了真正的信号）
- 健壮性不足（训练分布与实际需求偏离）

> 💡 **补充（课程原文引用）**：一篇系统研究数据质量影响的论文（《The Effects of Data Quality on Machine Learning Performance》）用 **15 种算法 × 5 折 × 6 个质量维度 × 3 种场景 = 1350 组实验**（课程原文写为 4050，实际计算为 1350）得出了可量化的结论。> 🌐 **交叉验证（arxiv 2207.14529 + ResearchGate）**：15 算法/6 维度/3 场景框架双源确认。

#### 3.3 各种污染机制的量化影响

| 污染类型 | 影响程度 | 关键发现 |
|---------|---------|---------|
| **标签错误** | **最直接** | 论文原文：20% 标签翻转 → F1 降 ≤10%（大多数算法）。课程原文"1%→2-5%"未直接确认，实际约为 0.5%/1%。当错误率 ≥ 1/类别数时模型"学不如猜"（二分类为 50%，5 类为 20%） |
| **特征噪声** | 线性衰减 | 小型数据集上 MLP 与 SVM 方差显著增大 |
| **缺失值** | 可容忍 | 训练时引入 ≤40% 缺失，模型可学会"容忍" |
| **重复样本** | 小样本致命 | 万级样本以上几乎无影响；<1k 样本时 5% 重复可导致 F1 掉 4-6% |
| **类别不平衡** | 存在临界点 | 少数类样本占比 < 1/类别数（即少数类数量 < 总样本数/类别数）时，性能开始显著下降——这不是断崖崩溃，而是风险加速恶化的预警线 |

**实践指导**：标签准确率 ≥80% 即可接受，不必追求 100% 人工重标；测试集必须人工二次校验；小数据集一定要先去重再训练。

---

## 🧠 本模块问题

**Q1**：SFT 训练时"只计算 output 部分的 loss"是一个关键设计选择。请解释：如果我们在 SFT 训练时对 **所有 token**（包括 instruction 和 input 部分）都计算 loss，会导致什么问题？提示——思考模型在这一设计下到底在学什么。

**Q2**：Alpaca 和 ChatML 是两种不同的数据格式。假设你正在构建一个客服机器人——用户可能多轮追问（"我的订单到哪了？""能改地址吗？""大概还要几天？"），同时需要一个系统级的人设（"你是专业、耐心、不说废话的客服"）。你应该选哪种格式？请说明理由并给出一个数据样例。

**Q3**：s1 论文用 1000 条高质量数据 + 26 分钟训练就在 MATH/AIME24 上超越 o1-preview 最高达 27%。但这是否意味着"数据越少越好"？在什么条件下"少量高质量数据"会失效？请给出至少一个具体的反例场景。

---

<!-- 学习者作答区（请在此处填写你的答案） -->

**A1**：

我们在之前的章节中已经学习到了 PPL 计算的 “长尾效应”，也就是 如果计算 instruction 和 input 部分的 loss，因为这部分数据概率值可能较高，而后续 output 部分概率输出较低，那么整体的 loss 会由 instruction 和 input 部分主导。
这显然不是我们想要的，我们希望模型能在 输入 满足条件的情况下，输出正确的 output，而不是让模型能在整体 instruction->input->output 上获得较低的 loss，而输出一个整体。


**A2**：

我们更应该选择 ChatML 格式。
ChatML 格式如下
```
{
  {"role": "system", "content":"你是专业、耐心、不说废话的客服"},
  {"role": "user", "content": "我的订单到哪了"},
  {"role": "assistant", "content": "xxxxxxxx"}
}
```
这种格式能满足当前题目的需求。

而 Alpaca 是 指令示的输入 和 输出，更适合工具调用、直接命令的方式。




**A3**：

s1 论文用了 1000 条高质量数据，但是按照文档所提示，这 1000 条高质量数据在 qwen-32B 上的 PPL 计算更低，因此可以判断 qwen-32B 基础训练数据中应该包含有这类数据内容，即 qwen-32B 模型本来就含有这 1000条 SFT 数据的分布。

在这样的条件下，再进行 SFT，能起到 "事半功倍" 的效果。

所以我们也可以判断，当这少量数据  质量不高、和模型预训练数据相距甚远（通过 模型 计算这部分数据 PPL 非常高）时，这种策略会失效。

比如模型中并没有包含某个小语种语言，或者某个量子物理相关的知识，我希望模型经过 一两条翻译小语种的"高质量" 数据，或者 解释该量子物理相关的知识，能让模型具备这种能力，明显是难以做到的。


---

<!-- 教师批改区（提交作业后由导师填写，请勿手动修改） -->

### 📝 批改结果

**Q1 批改**：你用"长尾效应"来解释——instruction/input 部分概率高会主导 loss——这个直觉方向是对的，但不是最核心的原因。

核心问题是**行为目标**：如果对所有 token 计算 loss，模型学的是"生成整个 instruction→input→output 序列"（即 P(全部|prefix)），而不是"给定输入，只生成输出"（即 P(output|input)）。推理时用户提供 instruction/input，模型不需要也不会去生成这些——但它被训练去预测它们，这会导致：
- 模型在推理时可能"续写用户的问题"而不是"回答"
- 浪费 capacity 去建模用户侧的 token 分布（这些在推理时本不归模型负责）

你说的"长尾效应"是损失数值层面的现象，但根本问题是**训练目标和推理行为的不一致**。— 得分：**6/10**

<details>
<summary>📖 Q1 参考答案</summary>

**核心问题：训练目标与推理行为失配**

如果对所有 token 计算 loss，模型学到的是：
```
P(instruction, input, output | prefix)  →  生成"整个对话"
```

但推理时我们需要的是：
```
P(output | instruction, input)  →  给定输入，只生成输出
```

**具体后果**：

1. **行为错乱**：模型学会了"预测用户会问什么"——推理时看到 prompt 后可能续写更多用户问题，而非切换为 assistant 角色回答。

2. **角色混淆**：模型无法区分"我应该复述的上下文"和"我应该生成的回答"。一个训练数据中 `user` 的 token 也被计算 loss，意味着模型被要求"同时扮演 user 和 assistant"。

3. **容量浪费**：模型的一部分 capacity 被用于建模"人类提问的分布"——这在推理时完全没用，因为 user 部分始终由用户提供。

**为什么只计算 output 部分是正确的**：

将 instruction/input 的 loss 权重设为 0，等价于告诉模型：
> "这些 token 只是上下文，你不需要预测它们。你唯一要学的是——看到这种格式后，assistant 应该说什么。"

这本质上将训练目标从"联合概率 P(all)"变为"条件概率 P(output|context)"，与推理时模型的角色完全一致。

**对比常见误解**：

| 误解 | 正确理解 |
|------|---------|
| "算全部 loss 会让模型学得更全面" | 模型会学会预测用户行为，反而干扰了真正的目标 |
| "长尾效应导致 loss 被高概率 token 主导" | 这是数值现象，根本原因是训练-推理目标失配 |
| "只算 output 是为了加速收敛" | 收敛快是副作用，真正目的是行为对齐 |

</details>

---

**Q2 批改**：选择 ChatML 正确，核心理由（system prompt + 多轮）点到。给的样例格式也正确。

可以补充的一个细节：为什么不是 ShareGPT？ShareGPT 通常不内置 system 角色——而"专业、耐心、不说废话的客服"这个人设恰好需要通过 system prompt 注入。ChatML 对此天然支持，ShareGPT 需要额外的格式 hack（如把 system prompt 伪装成第一轮 user 消息）。— 得分：**8/10**

<details>
<summary>📖 Q2 参考答案</summary>

**选择**：ChatML 格式。

**理由**：

1. **多轮对话**：客服场景天然多轮——用户追问、澄清、补充信息。ChatML 的 `messages` 数组直接表示对话历史，每一轮的角色关系清晰。

2. **System prompt**：题目要求注入人设（"专业、耐心、不说废话"），ChatML 原生支持 `system` 角色——这条消息在对话开始时作为"元指令"注入，始终作为所有后续轮次的上下文。

3. **Alpaca 不适用**：Alpaca 是单轮格式，`instruction` + `input` → `output`，无法表达"用户→客服→用户追问→客服再答"的多轮结构。虽然可以拼接多个 Alpaca 样本模拟多轮，但角色边界模糊，模型难以学会"何时轮到谁说话"。

**完整数据样例**：

```json
{
  "messages": [
    {"role": "system", "content": "你是专业、耐心、不说废话的客服。"},
    {"role": "user", "content": "我的订单到哪了？"},
    {"role": "assistant", "content": "您的订单已到达【XX分拨中心】，预计明天下午送达。需要我帮您查询具体配送时间吗？"},
    {"role": "user", "content": "能改地址吗？"},
    {"role": "assistant", "content": "订单未出库前可以修改。我查了一下，您的订单还在分拨中心，可以改。请提供新地址。"},
    {"role": "user", "content": "改完大概还要几天？"},
    {"role": "assistant", "content": "改地址后配送时间会重新计算，通常增加1-2天。您的预计新送达时间为后天。"}
  ]
}
```

注意几个关键设计点：
- System prompt 简洁明确——"不说废话"直接约束了回答风格
- 每轮 assistant 回复都体现"专业"（查了系统状态）+ "不说废话"（直接给结论）+ "服务意识"（主动提供下一步选项）
- 多轮之间有信息衔接（第二轮提到"订单还在分拨中心"被第三轮引用）

</details>

---

**Q3 批改**：你把 GRAPE 的洞察（PPL 低 = 与预训练分布匹配 → SFT 有效）作为解释框架，然后给出了两个清晰的反例场景，逻辑链路完整。两个反例（小语种翻译、量子物理）都抓住了核心——"少量数据生效的前提是模型已有相关能力，SFT 只做格式激活"。

两点可补充：(1) s1 的成功不单靠数据，还有 budget forcing 解码策略——数据策略和解码策略是组合拳，缺少任一个都会掉分；(2) 另一个反例场景——当任务需要**高度多样化的输出**（如创意写作、诗歌生成），少量同质化数据会严重坍缩输出的多样性，模型变成"只会写一种风格"。— 得分：**8/10**

<details>
<summary>📖 Q3 参考答案</summary>

**不，"数据越少越好"是一个危险的误读。**

s1 的成功有严格的隐含条件，这些条件被破坏时策略会失效：

**条件 1：数据必须与模型预训练分布匹配（GRAPE 原理）**

s1K 数据蒸馏自 Gemini-2.0-Flash-Thinking，而 Qwen2.5-32B-Instruct 本身在数学推理上有很强的预训练基础——这些数据之所以有效，不是因为"少"，而是因为"正好落在模型已有能力的激活区内"。如果切换为物理推理或医学诊断等模型薄弱的领域，同样的 1000 条数据效果会断崖下降。

**条件 2：任务必须是"格式激活型"而非"知识注入型"**

s1 的目标是让模型**展现已有的推理能力**（long chain-of-thought），不是注入新知识。对于"知识注入型"任务——如让小模型学会一个它从未接触过的专业领域（如专利法判断）——1000 条数据完全不够，需要大量数据来建立新的知识表示。

**条件 3：需要配套的推理策略**

s1 使用了 budget forcing——通过在推理时干预思考长度来提升效果。如果只用这 1k 数据训练但不做 budget forcing，效果会显著下降。数据策略和推理策略是组合拳。

**反例场景**：

| 场景 | 为什么失效 |
|------|----------|
| 为 Llama-7B 注入专利法知识（1k 条） | 模型预训练中专利法文本极少，1k 条无法建立新的知识表示 |
| 训练模型做斯瓦希里语翻译（1k 条） | 模型在预训练中几乎没见过该语种，缺少跨语言迁移的基础 |
| 训练模型写多样化诗歌（1k 条） | 1k 条同质化 → 输出多样性坍缩，所有诗都是一种风格 |
| 不做 budget forcing 的纯 s1 数据训练 | 效果大幅下降——s1 论文中数据和解码策略是组合生效的 |

</details>

---

**综合评价**：三个回答展现了较好的跨模块串联能力——Q1 引用长尾效应、Q3 引用 GRAPE 的 PPL 匹配原理，说明你在主动将新知识与已有框架建立联系。Q1 的"长尾效应"解释方向对但没命中核心（行为目标 vs 损失数值），Q2 选择正确但分析可更精炼，Q3 是最强的一题。继续这个"用旧知识解释新概念"的习惯，同时注意区分"表层机制"和"深层原因"。

**批改时间**：2026-06-03
