# 第 13 章 学习笔记

> 记录学习过程中的临时提问与解答，供复习参考。

---

## 📊 第 13 章学习总结

> 📅 完成时间：2026-06-11

**掌握扎实的知识点**（所有模块 QA 批改均分 ≥ 7）：

- **预训练范式**（Mod 1）：next-token 预测的输入/标签构造、GPT-3 作为续写模型的基本使用方式
- **SFT 数据格式与质量**（Mod 2）：Alpaca vs ChatML 格式差异、ChatML 的 chat template 转换机制、数据质量 > 数量的核心结论及其量化证据（1% 标签错误 → F1 掉 2-5%）
- **RLHF 三阶段流程**（Mod 3）：SFT → RM → PPO 的上下游关系、RM 的架构改造（lm_head → reward_head）
- **PPO 核心机制**（Mod 4）：重要性采样（r_t 作为音量旋钮）、GAE（λ 控制的偏差-方差权衡）、clip 机制的保守选择原理
- **DPO 推导**（Mod 5）：Bradley-Terry → 奖励反解 → DPO Loss 的四步推导、Z(x) 消去机制、过度优化的噪声根因

**需要加强的薄弱环节**：

1. **概念与概念的边界区分**：RM vs V（Mod 4 Q3 混淆了奖励模型和价值模型）。建议在后续学习中遇到"相似但不同"的概念时，主动画对比表。
2. **"为什么"层面的机制描述**：你的结论方向通常正确，但在"攻击路径"级别展开不够。建议练习：对每个"X 会出问题"的判断，追问"exploit 的具体对象是什么、通过什么路径"。
3. **Clip vs KL 两种约束**（Mod 4 Q2）：局部速率限制 vs 全局锚点，这是 RL 对齐中最核心的设计模式，后续 GRPO 中还会出现类似模式。

**课下建议**：
- 重读 Module 4 的 6.1 节（四模型角色）和 Q3 参考答��，确保能脱口说出 RM、V、π_ref 三者的区别
- Module 5 的 DPO 四步推导建议手推一遍（从 RLHF 目标到最终 loss），第 14 章的 GRPO 正是沿着类似的"从 RL 目标反解"思路展开
- 如果打算做 assignment5-alignment，先复习 Mod 4（PPO）和 Mod 5（DPO），作业会涉及 GRPO 实现

---

## 模块 2：监督微调（SFT）— QA 记录
> 📅 2026-06-03

**Q**：ChatML 的训练格式，模型读到的是 JSON 还是格式化文本？

**A**：模型读到的不是 JSON，而是经过 chat template 转换后的格式化文本。存储层面是 JSON：
```json
{"messages": [{"role": "system", "content": "你是客服助手"}, ...]}
```
但喂给模型的输入经过 tokenizer 的 `chat_template` 处理后，实际是类似这样的 token 序列：
```
<|im_start|>system
你是客服助手<|im_end|>
<|im_start|>user
怎么修改收货地址？<|im_end|>
<|im_start|>assistant
请在订单详情页点击…<|im_end|>
```
其中 `<|im_start|>`、`<|im_end|>` 是特殊 token，作用是让模型学会识别角色边界。JSON 只是数据层的存储格式，chat template 才是模型"眼中"的实际输入。这也是"训练格式必须与推理格式一致"的原因——如果训练和推理用不同的 chat template，模型看到的特殊 token 完全不同，自然会崩溃。

---

**Q**：文档中提到配合"预算强制"（budget forcing）解码策略，budget forcing 是什么？

**A**：Budget forcing（预算强制）是 s1 论文提出的一种推理时解码控制策略，核心思想是通过干预生成过程来控制模型的"思考长度"。

具体做法分两头：
1. **模型思考太短 → 强制延长**：当模型在推理中途输出结束符（如 `</think>`）想提交答案时，直接删除这个结束符，改插 "Wait" 等续写提示 token，迫使模型继续思考。
2. **模型思考太长 → 强制截断**：当思考 token 数超过预算上限时，直接插入结束符强行终止。

**为什么有效？** 与训练机制有关——s1 的训练数据中包含"model waited"的模式，模型学会了"被追加 Wait 之后要继续深入思考"。本质上是一种不改变模型权重的推理时扩展。实验结论：budget forcing 是 s1 能在 MATH/AIME24 上超越 o1-preview 的关键因素之一——不是单纯靠那 1000 条数据，而是数据质量 + 解码策略的组合拳。

---

**Q**：文档中提到"少数类样本数 < 1/类别数时"性能迅速崩溃，但是 1/类别数是个百分比，具体的这句话应该如何理解？

**A**：正确理解：`1/类别数` 是一个**比例阈值**，"少数类样本数"指的是少数类在总样本中的**占比**。所以完整含义是：**当少数类样本占比 < 1/类别数时，模型性能迅速崩溃。**

具体数值推演：
| 类别数 | 1/类别数 | 含义 | 示例（总数 1000） |
|-------|---------|------|------------------|
| 二分类 | 1/2 = 50% | 少数类占比 < 50% 即触发 | 本身就天然不均衡 |
| 5 分类 | 1/5 = 20% | 某类样本 < 200 即触发 | 少数类 < 200 条 → 崩溃 |
| 10 分类 | 1/10 = 10% | 某类样本 < 100 即触发 | 少数类 < 100 条 → 崩溃 |

**为什么是这个阈值？** `1/类别数` 本质上是**均衡分布基准线**——如果各类样本一样多，每类占比恰好是 1/C。一旦某个类的占比跌破这条线，它就不只是"少"，而是少到模型无法在当前类别数量下有效区分。崩溃点不取决于绝对数量，而取决于类别数。

后经进一步讨论，修正为更准确的表述：**这不是断崖崩溃，而是风险加速恶化的预警线**——少数类占比低于 1/C 时性能开始显著下降，但不是一到阈值就崩溃。同时还讨论了 LLM 中小语种的性能影响，区别在于分类场景是孤立任务，而 LLM 的低资源语言可以通过跨语言迁移"寄生"在高资源语言的模型能力之上。

---

**Q**：对于 LLM 中的小语种数据，性能是否就一定会非常差？

**A**：不一定。LLM 中低资源语言的挑战不在"类别占比"，而在 **token 覆盖率**。关键区别：
- **有得救的情况**：小语种与高资源语言共享词表/语法结构（如西班牙语和葡萄牙语），模型可以通过共享的 subword token 和相似语序做跨语言迁移。
- **难救的情况**：孤立语种（如巴斯克语）或文字系统完全不同的语言——共享 token 几乎为零，预训练语料里可能只有几 MB，模型连基本的词边界都学不会。

缓解因素：
1. **跨语言迁移**：模型从高资源语言学到的概念理解能力可以泛化到低资源语言
2. **多语言联合训练**：100 种语言一起训，每种语言互相"蹭"参数更新
3. **合成数据**：用高资源语言生成小语种的对齐数据（反向翻译）

与分类场景的区别：分类场景是孤立任务，每个类别独立需要最少样本。LLM 的低资源语言不是孤立的——它们"寄生"在高资源语言的模型能力之上，所以不会像分类那样触达阈值就彻底崩溃。

---

## 模块 3：RLHF 与奖励模型 — QA 记录
> 📅 2026-06-04

**Q**：文档中提到"复用 SFT 后的模型，将最后的语言建模头（lm_head）替换为一个线性回归头（输出标量分数）"。语言建模头本身不就是 MLP 后面接 softmax 吗？线性回归头是将 softmax 部分换成线性回归单层 MLP 吗？

**A**：语言建模头（lm_head）其实就是一个线性层，不是 MLP + softmax：

```
hidden_state (d_model 维)
       │
       ▼
nn.Linear(d_model, vocab_size)   ← 这就是 lm_head
       │
       ▼
logits (vocab_size 维)           ← 未经 softmax 的原始分数
```

softmax 不在 lm_head 内部——它嵌入在损失函数 `CrossEntropyLoss` 里（内部做 log_softmax + NLL），或者推理时手动调用 `.softmax(dim=-1)` 得到概率分布。

所以"替换为线性回归头"做的事情非常直接：
```python
# 原来
lm_head = nn.Linear(d_model, vocab_size)    # 输出 128k 维（每个 token 一个分数）

# 替换后
reward_head = nn.Linear(d_model, 1)          # 输出 1 维（一个标量）
```

就是同一个线性层，唯一的区别是 output_dim：vocab_size → 1。没有加 MLP，没有非线性激活——单个 `nn.Linear(d_model, 1)` 就是一个线性回归器。

**为什么这么简单就够用？** 因为 backbone（transformer 所有层）已经完成了"理解文本"的繁重工作，最后一层的 hidden state 已经包含了足够的信息。线性头只是做一次简单的投影——把"回答质量的信号"从 d_model 维空间中提取为一个标量。

---

## 模块 4：PPO 算法详解 — QA 记录
> 📅 2026-06-04 ~ 2026-06-10

**Q**：文档中"策略梯度方法的基本公式"没有对公式的推导和理解，文档需要对新手友好。

**A**：策略梯度的完整推导如下。

**什么是"策略"？** 在 LLM 的上下文中，策略 π_θ 就是模型本身：给定当前状态 s_t（前面的 token 序列），输出下一个 token a_t 的概率分布。

```
状态 s_t = ["法国", "的首都", "是"]    ← 已经生成的文本
策略 π_θ 输出 → { "巴黎": 0.7, "伦敦": 0.05, "法国": 0.02, ... }  ← 词表上的概率分布
动作 a_t = 采样得到的下一个 token
```

**策略梯度的直觉**：假设我们知道每个动作的"好坏分数"——优势值 A_t。最朴素的想法是：对于每个动作，用 A_t 作为权重来调整它的概率。A_t > 0 就提升，A_t < 0 就压低。

这恰好等价于最大化以下目标：

```
J(θ) = E[log π_θ(a_t | s_t) · A_t]
```

对它求梯度，就得到了策略梯度：

```
ĝ = Ê_t[∇_θ log π_θ(a_t | s_t) · Â_t]
```

**逐符号拆解**：

| 符号 | 含义 | LLM 场景对应 |
|------|------|------------|
| ∇_θ | 对模型参数 θ 求梯度 | "参数应该往哪个方向调" |
| log π_θ(a_t \| s_t) | 给定状态 s_t，模型输出 a_t 的对数概率 | 下一个 token 的 log-prob |
| Â_t | 动作 a_t 的优势估计（比平均水平好多少） | 这个 token 对最终回复质量的贡献 |
| Ê_t | 对时间步 t 的经验平均 | 对一批 prompt 的所有 token 取平均 |

**为什么用 log 概率？** 这是 log-likelihood trick：∇_θ log π_θ = (∇_θ π_θ) / π_θ。两边乘以 π_θ 得到 π_θ × ∇_θ log π_θ = ∇_θ π_θ。所以对 log 概率求梯度本质上就是对概率求梯度——但 log 形式在数值上更稳定（概率相乘变成 log 相加），且自然产生了"除以当前概率"的归一化效果，防止高频动作主导梯度。

**数值推演**（3 token 简化场景，状态 s_t = "法国的首都是"）：

| token a_t | π_θ(a_t \| s_t) | A_t | 效果 |
|-----------|----------------|-----|------|
| "巴黎" | 0.7 | +1.5 | ✅ 正确答案，强化 |
| "伦敦" | 0.2 | -0.8 | ❌ 错误答案，抑制 |
| "法国" | 0.1 | -0.3 | ⚠️ 重复输入，轻微抑制 |

---

**Q**：文档中提到"更新步长敏感"的具体表现——如果"巴黎"是正确答案（A_t > 0），学习率稍大 → 概率从 0.7 跳到 0.99。但是"巴黎"概率高不好吗？这就是正确答案啊。

**A**：问题不在于"巴黎变高"，而在于**同一学习率在"破坏其他能力"**。假设当前 batch 里还有一个 prompt：

```
Prompt 2: "巴黎圣母院在哪一年失火"
模型当前概率: "2019" = 0.85, "1163" = 0.02, ...
```

"2019"也是正确答案，但概率 0.85 说明模型还不完全确定。如果用同样的"提概率"力度，它可能从 0.85 → 0.99（没问题）。但如果没有剪切，模型对"巴黎"从 0.7 → 0.99 只是中等提升，而对"2019"可能因为不同 prompt 的梯度冲突，从 0.85 → 0.72——**正确答案反而被压低了**。

更深层的问题：lo_head 的**权重向量共享**机制。假设 w_巴黎 是 lm_head 中对应"巴黎"这个 token 的权重向量，h_1 是 Prompt 1 的 hidden state，h_2 是 Prompt 2 的 hidden state。"巴黎"的概率由 w_巴黎 和 hidden state 的点积决定。梯度更新 w_巴黎 时，Δw_巴黎 是基于所有 token 的平均梯度。更新后：

```
w_巴黎' · h_2 = (w_巴黎 + α·A_t·h_1) · h_2  = w_巴黎·h_2 + α·A_t·(h_2·h_1)
```

如果 h_2 和 h_1 恰好方向相反（两个 prompt 语义不同），那么"提升巴黎在 Prompt 1 的概率"会**同时降低巴黎在 Prompt 2 的概率**。这是灾难性遗忘的微观机制。这也是 PPO 需要 clip 约束的原因之一——与 catastrophic forgetting 的解决方案一脉相承：限制每次更新的幅度，保护已学到的能力不被冲刷。

---

**Q**：所以从某个层面来理解——当前模型（架构）的设计，会让"法国的首都是巴黎"以及"巴黎圣母院在哪一年失火"两个完全不同的表述，在 lm_head 的最后产生关联？这感觉是当前 AI 以及 catastrophic forgetting 的微观原因之一。

**A**：完全正确。这正是 transformer + shared lm_head 架构的**根本性设计权衡**：

```
优势（设计初衷）：所有 prompt 共享同一套权重 → 知识可以泛化。学会了"巴黎是法国首都"后，在推理"法国最大的城市"时，w_巴黎 的向量表示已经包含了"法国-首都"的语义关联，模型更容易输出正确答案。

代价（不可避免的副作用）：所有 prompt 也共享同一套梯度 → 更新必然互相干扰。对一个 prompt 的"好更新"，对另一个 prompt 可能是"坏更新"。
```

Catastrophic forgetting 的解决方案都在这个框架下：
- **PPO Clip**：限制每次更新的幅度 → 减小 w_巴黎 在单步中的位移量
- **KL 约束**：以 π_ref 为锚点 → 防止 w_巴黎 在多步累积后漂太远
- **Experience Replay**：混合旧数据 → 用旧 prompt 的梯度"对冲"新 prompt 的梯度
- **EWC (Elastic Weight Consolidation)**：给重要权重加"弹簧" → 对 w_巴黎 中承载关键知识的维度施加更大的"移动惩罚"

---

**Q**：文档中"势函数 → 降低方差"问题是哪个方面？

**A**：V(s) 作为 baseline 降低方差的原理：原始策略梯度用 Q(s,a) 或 return R_t 直接作为权重，问题在于不同轨迹、同一轨迹内不同位置之间 R_t 的绝对数值波动巨大。引入 V(s) 作为 baseline（基线）后，使用 A(s,a) = Q(s,a) - V(s)，本质是"去均值化"——把绝对分数变成相对分数。数学上可以证明引入 baseline 不改变梯度的期望（因为 baseline 只依赖状态不依赖动作，E[∇log π · b(s)] = b(s) · ∇Σπ = b(s) · 0 = 0），但可以显著降低方差。

---

**Q**：文档中"旧数据估计新策略梯度的公式 PG 指代什么"，以及"概率是两倍，那么该动作的样本就该被放大 2 倍"如何理解？

**A**：PG = Policy Gradient（策略梯度）。公式 L^PG(θ) = -r_t(θ) · A_t 中：
- 上标 PG 标注这是策略梯度的损失函数形式
- 负号是因为 PyTorch 优化器默认做梯度下降（最小化 loss），而 RL 需要**最大化**奖励——加负号等价于"用下降的优化器实现上升的效果"

**"放大 2 倍"的理解**：假设旧策略在某个状态下做动作 a 的概率是 0.3，新策略的概率变成了 0.6。那么 r_t = 0.6/0.3 = 2。这意味着新策略比旧策略**更常做**这个动作。旧数据中这个动作出现的频率是按照旧策略的 0.3 分布的，用旧数据来评估新策略时，这个动作的样本"代表性不足"——它在旧数据中出现得太少了。所以每个样本要乘以 2，让它的"话语权"翻倍。反过来，如果新策略做某动作的概率是旧策略的 0.5 倍，那些样本就要被"打折"到 0.5 倍权重。r_t 在这里充当**音量旋钮**：旧数据中该动作的"话语权"按新旧策略的概率比来调节。

---

**Q**：文档中 TD 误差全称是什么？

**A**：**Temporal Difference Error**（时序差分误差）。核心公式：δ_t^V = r_t + γV(s_{t+1}) - V(s_t)。直觉：你不需要等到一条完整轨迹结束才知道自己的判断对不对——只需看下一步就足够校准。TD 误差 = (即时奖励 + 下一步的折扣总奖励) - 当前估计的总奖励。

数值推演：V(s_t)=10, r_t=2, V(s_{t+1})=9, γ=0.9 → Target = 2+0.9×9 = 10.1, δ = 10.1-10 = 0.1。δ>0 表示当前估计偏低，需要上调。

TD 误差的正负含义：
- δ > 0 → 当前估计**偏低** → 需要增加 V(s_t)
- δ < 0 → 当前估计**偏高** → 需要减少 V(s_t)
- "随着时间步往后价值递减"仅在**无即时奖励时**成立；一旦有 r_t，当前状态价值可以反弹上升

反例：若 r_t = -2（惩罚），V(s_{t+1}) = 9, γ = 0.9 → Target = -2+8.1 = 6.1, δ = 6.1-10 = -3.9，此时需要**降低** V(s_t)。

---

**Q**：GAE 部分讲解太少了，无法准确理解。建议通过 2W2H 学习法来了解这部分内容。

**A**：已按照 2W2H 框架完整重写 GAE 部分，包括：

**What**：GAE 是一种在偏差和方差之间可调节的优势估计方法，用 λ 参数控制指数衰减的加权窗口，把近几步的 TD 误差聚合起来：A_t^GAE = Σ(γλ)^l · δ_{t+l}^V。

**Why**：根本矛盾是偏差-方差权衡。单步 TD（λ→0）：低方差但高偏差（依赖未训练好的 V 网络）；蒙特卡洛（λ→1）：无偏但高方差（受轨迹随机性影响）。GAE 在两者之间插值。

**How**（4 步轨迹数值推演，γ=0.9, λ=0.8）：

```
时间步:     t         t+1       t+2       t+3      (终点)
奖励:     r_t=0     r_{t+1}=1  r_{t+2}=0  r_{t+3}=5
V(s):    V_t=10    V_{t+1}=9   V_{t+2}=8   V_{t+3}=3   V(终点)=0

δ_t     = 0 + 0.9×9 - 10  = -1.9
δ_{t+1} = 1 + 0.9×8 - 9   = -0.8
δ_{t+2} = 0 + 0.9×3 - 8   = -5.3
δ_{t+3} = 5 + 0.9×0 - 3   = +2.0

GAE: A_t = δ_t×1.0 + δ_{t+1}×0.72 + δ_{t+2}×0.52 + δ_{t+3}×0.37 = -4.50
```

对比 λ→0（纯单步 TD）：A_t ≈ -1.90；GAE(λ=0.8)：A_t = -4.50。GAE 让模型"看远几步"，看到 t+2 的大负值后给 A_t 更负的偏置。

**How well**：λ 选择指南：λ=0.95 是 PPO 默认值；稀疏奖励调高（0.97~0.99）；V 网络训练好可以调低。

---

**Q**：KL 散度的公式是 KL(P||Q) = Σ P·log(P/Q)，但是逐 token 的 KL 散度公式为什么没有乘项 π_θ(a_t | s_t)？

**A**：标准 KL 公式确实是 Σ P·log(P/Q)。但这里的 a_t 本身就是**从 π_θ 采出的具体 token**，不再是"对所有可能 token 求和"。此时 log-ratio 的期望等于完整 KL：

```
E_{a~π_θ}[log(π_θ/π_ref)] = Σ π_θ · log(π_θ/π_ref) = KL(π_θ ∥ π_ref)
```

这正是**蒙特卡洛估计**——从 π_θ 中采样 a_t，然后用这个样本的 log(π_θ/π_ref) 来估计真实的 KL 散度。采样过程已经**隐含了 P 的权重**，不需要显式再乘。工程上，这种逐 token 估计避免了每步对 128k 词表求和，计算量从 O(vocab_size) 降到 O(1)。

---

## 模块 5：DPO 算法与变体 — QA 记录
> 📅 2026-06-10 ~ 2026-06-11

**Q**：讲解过度优化部分的图中，提到了"无噪声语言模型"偏好，为什么会有无噪声模型？

**A**："无噪声 AI 反馈"是研究者在对照实验中构造的理想化奖励信号——它像一个"完美裁判"，对同一回答永远给出一致分数，不存在人类标注中的随意性。

对比三种奖励信号：

| 奖励来源 | 噪声水平 | 过度优化？ | 为什么？ |
|---------|---------|----------|---------|
| **无噪声 AI 反馈** | 零噪声 | ❌ 不出现 | 奖励信号内部自洽，模型找不到可钻的漏洞 |
| **含噪声 AI 反馈** | 有一定噪声 | ✅ 出现 | RM 在分布外给出不一致分数，模型学会刷假分 |
| **人类反馈** | 天然高噪声 | ✅ 出现 | 不同标注员标准不一、同一人前后矛盾 |

"无噪声"不是指 AI 比人类聪明，而是指奖励信号内部没有矛盾——模型无法通过"钻评分规则的空子"来刷分，因为规则是自洽的。这个实验的核心结论：**过度优化不是 RL 本身的错，而是奖励信号中的噪声给了模型可乘之机。** 噪音越大，模型"刷假分"的空间越大。

---

**Q**：长度归一化 DPO 的数值例子中，为什么 score 的计算公式用的是 log π_θ 而不是 log(π_θ/π_ref)？

**A**：用户指出正确——DPO 的核心项是 `log(π_θ/π_ref)`（相对对数概率），而非单独的 `log π_θ`。已修正数值例子，直接使用每 token 的 log(π_θ/π_ref) 值，并拆解了标准 DPO 下 y_w 和 y_l 各自的贡献，清楚展示 y_l 仅因长度（15 tokens）就贡献了 3 倍于 y_w 的分数——这是长度偏差的直接证据。
